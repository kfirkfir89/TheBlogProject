@using Microsoft.AspNetCore.Identity
@using TheBlogProject.Services
@using TheBlogProject.ViewModels

@inject SignInManager<BlogUser> SignInManager
@inject UserManager<BlogUser> UserManager
@inject IImageService _imageService


<div class="row">
    @if (SignInManager.IsSignedIn(User))
    {
        BlogUser blogUser = (BlogUser)await UserManager.GetUserAsync(User);


        <div class="btn-group d-flex d-inline-block">

            <a class="btn btn-primary logoutButton border-1 px-2 pt-2 me-2 shadow-none d-none d-md-block" asp-controller="Posts" asp-action="Create">
                <span class="aTextStyle ">CREATE POST</span>
            </a>
            <a class="btn btn-primary logoutButton border-1 px-2 pt-2 me-2 shadow-none d-none d-md-block" asp-area="Identity" asp-area="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <span class="aTextStyle ">CREATE </span>
            </a>
            <form class="d-none d-md-block" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button type="submit" class="btn btn-primary logoutButton border-1 px-4 shadow-none">LOGOUT</button>
            </form>
        </div>

    }
</div>
